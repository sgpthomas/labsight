# This is not only the main, but also the place where all the global variables are stored.
# Here position and everyting can be calculated, as this class provides the reference point for all the other classes

class Main
    def __init__():
        if setup_yet == False:
            a = Setup()
        else:
            load_vars()
    def load_vars():

    def save_vars():

    def steps_to_cent(steps):

    def cent_to_steps(cent):
